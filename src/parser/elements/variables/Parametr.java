package parser.elements.variables;

import parser.elements.identifiers.Identifier;

public class Parametr extends Attribute {
    public Parametr(Identifier identifier, double value) {
        super(identifier, value);
    }
}
